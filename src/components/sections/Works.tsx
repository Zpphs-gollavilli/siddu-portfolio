// Works.jsx

// This file is intentionally disabled to remove the Projects section.
// You can safely import it elsewhere without breaking the app.

const Works = () => {
  return null; // Nothing will be rendered
};

export default Works;
